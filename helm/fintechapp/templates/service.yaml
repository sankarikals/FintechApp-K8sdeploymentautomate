apiVersion: v1
kind: Service
metadata:
  name: {{ include "fintechapp.fullname" . }}
  labels:
    {{- include "fintechapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fintechapp.selectorLabels" . | nindent 4 }}
